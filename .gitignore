# Python
__pycache__/
*.pyc
.venv/
.env

# Build / cache artifacts
unsloth_compiled_cache/
.pytest_cache/

# Fine-tuned model weights — the merged model is ~15GB (fp16
# safetensors), far too large for git. Reproduce by running
# finetuning/train_qlora.py (see README) — trains from the synthetic
# dataset already included in finetuning/data/.
finetuning/parse_query_gguf/

# Internal planning/decision logs — not part of the distributed project
DECISIONS.md
PROJECT_CONTEXT.md
PIVOT_CONTINUATION_PROMPT.md
finetuning/PLAN.md

# Next.js dev-agent scaffold files (auto-regenerated by `next dev`,
# not needed to run or understand the project)
frontend/AGENTS.md
frontend/CLAUDE.md

# Claude Code project-local config
.claude/
